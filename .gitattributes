# Normalize line endings to LF in the repo (the site deploys on Linux/Vercel).
* text=auto eol=lf

# Treat binary assets as binary (no EOL conversion, no diff noise).
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.mp4 binary
*.pdf binary
*.ico binary
